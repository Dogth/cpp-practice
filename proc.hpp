#ifndef PROC_HEADER
#define PROC_HEADER

std::pair<double, double> solveProc3(auto&[x1, y1], auto&[x2, y2]);

bool solveProc26(double value);

std::array<int, 3> solveProc50(int seconds);

double solveProc56(auto&[x1, y1], auto&[x2, y2]);

#endif
